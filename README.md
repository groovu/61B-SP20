# Minature Git - Gitlet
Gitlet is a clone of git (with less features), written in Java.  

Features the following commands: init, add, commit, rm, log, global-log, find, status, checkout, branch, rm-branch, reset, merge commands.

# todo 
Video demo

Rewrite in another language?
